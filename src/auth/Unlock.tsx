import * as React from 'react';
import { Storage } from '../storage/Storage';

export const Unlock = React.memo((props: { storage: Storage, onReady: (storage: Storage) => void }) => {
    return null;
});